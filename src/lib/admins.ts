// /src/lib/admins.ts

/**
 * Lista de UIDs de usuários que têm acesso à área de administração.
 * Adicione aqui os UIDs dos usuários do Firebase Authentication que devem ser administradores.
 * 
 * Para encontrar o UID de um usuário:
 * 1. Vá para o seu projeto no Firebase Console.
 * 2. No menu "Build", clique em "Authentication".
 * 3. Na aba "Users", você verá a lista de todos os usuários.
 * 4. O UID é a string de caracteres na coluna "User UID".
 */
export const ADMIN_USER_UIDS: string[] = [
    "yWgXBE21hqhiLlK97DztPvg4VfQ2",
];
